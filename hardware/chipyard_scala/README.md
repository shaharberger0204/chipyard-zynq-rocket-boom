# Final Chipyard Scala target

The authoritative board-specific source is `zynq7cg/` and contains exactly the four final Scala files supplied for the project:

- `Configs.scala`
- `HarnessBinders.scala`
- `TestHarness.scala`
- `Zynq7CGShell.scala`

Older duplicated `rocket/` / `boom/` Scala snapshots in the pre-existing GitHub repository should be archived or removed if they differ from this final target.
